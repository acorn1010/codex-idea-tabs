export type Json = { [key: string]: unknown };
export type Attachment = { name: string; path: string; mime: string; size: number };
export type Input = { type: 'text'; text: string } | { type: 'localImage'; path: string } | { type: 'image'; url: string } | { type: 'skill' | 'mention'; name: string; path: string };
export type Item = Json & { id: string; type: string; text?: string; content?: Json[]; command?: string; aggregatedOutput?: string; status?: string; changes?: Json[]; summary?: string[] };
export type Question = { id: string; header?: string; question: string; isSecret?: boolean; options?: { label: string; description?: string }[] };
export type Pending = Json & { key: string; method: string; rpcId?: string | number; questions?: Question[]; command?: string; reason?: string; message?: string; requestedSchema?: Json };
export type Chat = { id: string; title: string; cwd: string; threadId: string; draft: string; draftAttachments?: Attachment[]; draftInput?: Input[]; historyCursor?: string; items: Item[]; requests: Pending[]; status: 'idle' | 'working' | 'attention' | 'complete' | 'error'; working: boolean; unread: boolean; pinned: boolean; archived?: boolean; error?: string; diff?: string; revision: number; tokenUsage?: Json; plan?: { step: string; status: string }[]; partial?: boolean };
export type Model = { id: string; model: string; displayName: string; defaultReasoningEffort: string; supportedReasoningEfforts: { reasoningEffort: string; description: string }[]; isDefault?: boolean };
export type Snapshot = { chat: Chat; sessions: Chat[]; models: Model[]; account: { account?: { type: string; email?: string; planType?: string }; requiresOpenaiAuth?: boolean }; connection: string; error: string; distro: string; cwd: string; project: string; theme?: 'dark' | 'light'; settings: { model: string; effort: string; permissions: string } };
